pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/zeeshaan02/jenkins012.git'
            }
        }

        stage('Deploy') {
            steps {
                sh 'ansible-playbook zee.yml -i /etc/ansible/hosts'
            }
        }

        stage('Verify') {
            steps {
                sh 'ansible-playbook deploy.yml -i /etc/ansible/hosts'
            }
        }
    }
}

